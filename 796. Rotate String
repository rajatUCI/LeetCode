class Solution {
public:
    bool rotateString(string A, string B) {
        if(A.size() != B.size())
            return false; 
        string result = A + A;   
        if(result.find(B) != -1)
            return true;
        return false;
    }
};